import React from 'react'
import {View, Text, TouchableOpacity, StyleSheet} from 'react-native'

const Quiz = ({question}) => (
    <View>
        <Text>Quiz</Text>
    </View>
)

export default Quiz
